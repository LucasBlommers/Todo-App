<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todoapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodoApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name="eu.lucasblommers.todoapp.activities.CreateTodoCollectionActivity"
            android:exported="true" />
        <activity
            android:name="eu.lucasblommers.todoapp.activities.TodoCollectionActivity"
            android:exported="true" />
        <activity
            android:name="eu.lucasblommers.todoapp.activities.RegisterActivity"
            android:exported="true" />
        <activity
            android:name="eu.lucasblommers.todoapp.activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>